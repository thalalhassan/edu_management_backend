-- Drop the RoleSnapshot table if it exists
DROP TABLE IF EXISTS role_snapshot;
-- Drop the Role table if it exists
DROP TABLE IF EXISTS roles;
-- Drop the Permission table if it exists
DROP TABLE IF EXISTS permission;
-- Drop the UserScope table if it exists
DROP TABLE IF EXISTS user_scope;
-- Drop the UserRefreshToken table if it exists
DROP TABLE IF EXISTS user_refresh_token;